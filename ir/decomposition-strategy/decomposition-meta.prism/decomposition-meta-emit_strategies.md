---
authored_by: decomposition-strategy-prism-decomposer
authored_on: '2026-04-29T22:56:59.437Z'
authored_via: outside
authority_level: convention
collapsed_summary: 'Decomposition-strategy workflow node ''emit_strategies''. Resolver: meta.emit-strategies.'
depends_on: []
expanded_into: null
id: decomposition-meta-emit_strategies
kind: ir-node
parent: decomposition-meta
projection_types: []
resolution_event: 01KQDQBP9RZ7F7XKM0GWRN2P10
resolved_at: '2026-04-29T22:57:06.872Z'
resolver: meta.emit-strategies
revalidate_trigger: null
scope: decomposition-strategy
status: resolved
superseded_by: null
supersedes: null
surrogate_of: null
tier: 1
valid_through: null
visible_to:
- decomposition-strategy
---

# Intention

Decomposition-strategy workflow node 'emit_strategies'. Resolver: meta.emit-strategies.

```yaml
prism_operator:
  op: script
  resolver: meta.emit-strategies
  model: null
```

# Resolution

{"meta_id": "decomposition-strategy", "seed_input_path": "seed/notes-on-substrate-composition.md", "seed_bytes": 6813, "seed_sha256": "809731bbfffb4642d945b8b29cc7e597e57274da5ca329070f809aaa895ffb8a", "domain_for_strategies": "decomposition-strategy-prism-decomposer", "emissions": [{"discriminator": "token-count", "program_id": "strategy.token-count", "collapsed_summary": "token-count decomposition strategy \u2014 emitted by meta.emit-strategies for seed seed/notes-on-substrate-composition.md", "intention_body": "# Intention\n\nApply the token-count decomposition strategy to a fixed seed input. This program was emitted by `meta.emit-strategies` as part of the decomposition-strategy demo's self-composition trace. The 8OS frontmatter (above this Intention section, authored by the orchestrator at Phase 2) and the PRISM-IR body `id` (below) match exactly per v1.1 identity discipline: both are `strategy.token-count`.\n\nThe chunker resolver `chunker.token-count` is registered in the host 8OS instance as a deterministic Python implementation. Calling it with the params declared below produces a list of chunk-boundary records as `resolution_value.chunks`, in a fixed shape shared across all three strategies (so the comparison resolver can read them uniformly).\n\n```yaml\nv: 1.1.0\nprism: strategy.token-count\nversion: 1.1.0\nconformance: level-1\nid: strategy.token-count\nname: Token Count decomposition strategy\ndomain: decomposition-strategy/token-count\nintention: Decompose the seed input at 'seed/notes-on-substrate-composition.md' into\n  chunks using the token-count strategy. Emit chunk boundaries as character offsets\n  into the seed plus per-chunk summaries. The chunks are not interpreted further by\n  this program; the comparison resolver consumes them downstream.\nfailure_tolerance:\n  chunk: retry\nconstraints:\n- sla: total flow under 5s\n  fail: drop\n  priority: low\nparams:\n  seed_input_path: seed/notes-on-substrate-composition.md\n  tokens_per_chunk: 200\nentities:\n- id: chunk_state\n  fields:\n  - chunks\nnodes:\n- id: start\n  t: start\n- id: chunk\n  t: task\n  o:\n    op: script\n    resolver: chunker.token-count\n  out:\n  - chunks\n- id: end\n  t: end\nedges:\n- s: start\n  t: chunk\n- s: chunk\n  t: end\nmetrics:\n- id: chunk_count\n  expr: length(chunk_state.chunks) at end\n```\n\n## Resolver semantics (informational)\n\nThe single `op: script, resolver: chunker.token-count` declaration above binds to a deterministic Python implementation registered as a `_kernel.resolver` record at `8os/ir/_kernel/resolver/chunker.token-count.md`. The implementation lives in the demo repo's `harness/resolvers/` directory. The chunker reads the seed text from disk, applies its strategy, and returns the chunks as `resolution_value`.\n\n## Hosting note\n\nThis program was emitted by `meta.emit-strategies` and authored as a tier-1 (I, R) by the demo's orchestrator at Phase 2. It did not exist before Run 1; it was the resolution of another PRISM-IR program. Run 2 (executed after this record was authored) decomposes and dispatches it.\n"}, {"discriminator": "structural", "program_id": "strategy.structural", "collapsed_summary": "structural decomposition strategy \u2014 emitted by meta.emit-strategies for seed seed/notes-on-substrate-composition.md", "intention_body": "# Intention\n\nApply the structural decomposition strategy to a fixed seed input. This program was emitted by `meta.emit-strategies` as part of the decomposition-strategy demo's self-composition trace. The 8OS frontmatter (above this Intention section, authored by the orchestrator at Phase 2) and the PRISM-IR body `id` (below) match exactly per v1.1 identity discipline: both are `strategy.structural`.\n\nThe chunker resolver `chunker.structural` is registered in the host 8OS instance as a deterministic Python implementation. Calling it with the params declared below produces a list of chunk-boundary records as `resolution_value.chunks`, in a fixed shape shared across all three strategies (so the comparison resolver can read them uniformly).\n\n```yaml\nv: 1.1.0\nprism: strategy.structural\nversion: 1.1.0\nconformance: level-1\nid: strategy.structural\nname: Structural decomposition strategy\ndomain: decomposition-strategy/structural\nintention: Decompose the seed input at 'seed/notes-on-substrate-composition.md' into\n  chunks using the structural strategy. Emit chunk boundaries as character offsets\n  into the seed plus per-chunk summaries. The chunks are not interpreted further by\n  this program; the comparison resolver consumes them downstream.\nfailure_tolerance:\n  chunk: retry\nconstraints:\n- sla: total flow under 5s\n  fail: drop\n  priority: low\nparams:\n  seed_input_path: seed/notes-on-substrate-composition.md\n  heading_levels:\n  - '#'\n  - '##'\n  - '###'\nentities:\n- id: chunk_state\n  fields:\n  - chunks\nnodes:\n- id: start\n  t: start\n- id: chunk\n  t: task\n  o:\n    op: script\n    resolver: chunker.structural\n  out:\n  - chunks\n- id: end\n  t: end\nedges:\n- s: start\n  t: chunk\n- s: chunk\n  t: end\nmetrics:\n- id: chunk_count\n  expr: length(chunk_state.chunks) at end\n```\n\n## Resolver semantics (informational)\n\nThe single `op: script, resolver: chunker.structural` declaration above binds to a deterministic Python implementation registered as a `_kernel.resolver` record at `8os/ir/_kernel/resolver/chunker.structural.md`. The implementation lives in the demo repo's `harness/resolvers/` directory. The chunker reads the seed text from disk, applies its strategy, and returns the chunks as `resolution_value`.\n\n## Hosting note\n\nThis program was emitted by `meta.emit-strategies` and authored as a tier-1 (I, R) by the demo's orchestrator at Phase 2. It did not exist before Run 1; it was the resolution of another PRISM-IR program. Run 2 (executed after this record was authored) decomposes and dispatches it.\n"}, {"discriminator": "density", "program_id": "strategy.density", "collapsed_summary": "density decomposition strategy \u2014 emitted by meta.emit-strategies for seed seed/notes-on-substrate-composition.md", "intention_body": "# Intention\n\nApply the density decomposition strategy to a fixed seed input. This program was emitted by `meta.emit-strategies` as part of the decomposition-strategy demo's self-composition trace. The 8OS frontmatter (above this Intention section, authored by the orchestrator at Phase 2) and the PRISM-IR body `id` (below) match exactly per v1.1 identity discipline: both are `strategy.density`.\n\nThe chunker resolver `chunker.density` is registered in the host 8OS instance as a deterministic Python implementation. Calling it with the params declared below produces a list of chunk-boundary records as `resolution_value.chunks`, in a fixed shape shared across all three strategies (so the comparison resolver can read them uniformly).\n\n```yaml\nv: 1.1.0\nprism: strategy.density\nversion: 1.1.0\nconformance: level-1\nid: strategy.density\nname: Density decomposition strategy\ndomain: decomposition-strategy/density\nintention: Decompose the seed input at 'seed/notes-on-substrate-composition.md' into\n  chunks using the density strategy. Emit chunk boundaries as character offsets into\n  the seed plus per-chunk summaries. The chunks are not interpreted further by this\n  program; the comparison resolver consumes them downstream.\nfailure_tolerance:\n  chunk: retry\nconstraints:\n- sla: total flow under 5s\n  fail: drop\n  priority: low\nparams:\n  seed_input_path: seed/notes-on-substrate-composition.md\n  window_sentences: 5\n  unique_word_ratio_threshold: 0.55\nentities:\n- id: chunk_state\n  fields:\n  - chunks\nnodes:\n- id: start\n  t: start\n- id: chunk\n  t: task\n  o:\n    op: script\n    resolver: chunker.density\n  out:\n  - chunks\n- id: end\n  t: end\nedges:\n- s: start\n  t: chunk\n- s: chunk\n  t: end\nmetrics:\n- id: chunk_count\n  expr: length(chunk_state.chunks) at end\n```\n\n## Resolver semantics (informational)\n\nThe single `op: script, resolver: chunker.density` declaration above binds to a deterministic Python implementation registered as a `_kernel.resolver` record at `8os/ir/_kernel/resolver/chunker.density.md`. The implementation lives in the demo repo's `harness/resolvers/` directory. The chunker reads the seed text from disk, applies its strategy, and returns the chunks as `resolution_value`.\n\n## Hosting note\n\nThis program was emitted by `meta.emit-strategies` and authored as a tier-1 (I, R) by the demo's orchestrator at Phase 2. It did not exist before Run 1; it was the resolution of another PRISM-IR program. Run 2 (executed after this record was authored) decomposes and dispatches it.\n"}]}
