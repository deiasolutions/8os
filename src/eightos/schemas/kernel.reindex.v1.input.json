{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kernel.reindex.v1.input",
  "title": "kernel.reindex input",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode"],
  "properties": {
    "mode": { "enum": ["rebuild", "check"] }
  }
}
