{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kernel.ir.promote.v1.error",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "op", "status", "code", "message", "context"],
  "properties": {
    "schema_version": { "const": 1 },
    "op": { "const": "kernel.ir.promote" },
    "status": { "const": "error" },
    "code": { "enum": ["SCHEMA_INVALID", "NOT_FOUND", "ALREADY_EXISTS", "INVALID_STATE"] },
    "message": { "type": "string" },
    "context": { "type": "object" }
  }
}
