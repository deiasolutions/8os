{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kernel.authorize.v1.error",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "op", "status", "code", "message", "context"],
  "properties": {
    "schema_version": { "const": 1 },
    "op": { "const": "kernel.authorize" },
    "status": { "const": "error" },
    "code": { "enum": ["SCHEMA_INVALID", "AUTHORITY_INSUFFICIENT", "NOT_FOUND"] },
    "message": { "type": "string" },
    "context": { "type": "object" }
  }
}
