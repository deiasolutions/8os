{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kernel.ir.expand.v1.error",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "op", "status", "code", "message", "context"],
  "properties": {
    "schema_version": { "const": 1 },
    "op": { "const": "kernel.ir.expand" },
    "status": { "const": "error" },
    "code": { "enum": ["SCHEMA_INVALID", "INVALID_STATE", "NOT_FOUND", "ATOMICITY_FAILURE", "ALREADY_EXISTS"] },
    "message": { "type": "string" },
    "context": { "type": "object" }
  }
}
