{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kernel.ir.supersede.v1.output",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "op", "status", "data", "event_id", "indexes_updated"],
  "properties": {
    "schema_version": { "const": 1 },
    "op": { "const": "kernel.ir.supersede" },
    "status": { "const": "ok" },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "required": ["old_ir_id", "new_ir_id", "new_path"],
      "properties": {
        "old_ir_id": { "type": "string" },
        "new_ir_id": { "type": "string" },
        "new_path": { "type": "string" }
      }
    },
    "event_id": { "type": "string" },
    "indexes_updated": { "type": "array", "items": { "type": "string" } }
  }
}
