"""JSON Schema definitions for every SDK operation.

Files are named `<op>.v<n>.{input,output,error}.json`. The kernel ships the
canonical copy here; `kernel.init` vendors them into `.8os/sdk/schemas/` per
repo as the binding contract record for that repo's kernel ABI.
"""
